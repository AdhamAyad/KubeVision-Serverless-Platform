apiVersion: v1
kind: ConfigMap
metadata:
  name: config-gateway
  namespace: knative-serving
data:
  gateway-api.external-gateway: "kubevision-ingress/kubevision-gateway"
  gateway-api.local-gateway: "kubevision-ingress/kubevision-gateway"
  gateway-api.section-name: "https-wildcard"